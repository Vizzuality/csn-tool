<h1><%= @species.scientificname %></h1>

<div class="content" style="width:100%;display:flex;flex-wrap:wrap;">
  <div class="map" id="map" style="width:100%;height:300px;position:relative;"></div>
  <div  class="table" style="width:100%">
    <table>
      <thead>
        <tr>
          <th>Name</th>
          <th>Country</th>
          <th>Site rec id</th>
          <th>Actions</th>
        </tr>
      </thead>
      <tbody>
        <% @sites.each do |site| %>
          <tr>
            <td><%= link_to site.intname, site.hyperlink, target: "_blank" %></td>
            <td><%= site.country %></td>
            <td><%= site.sitrecid %></td>
            <td><%= link_to "view details", site_path(site.sitrecid) %></td>
          </tr>
        <% end %>
    </table>
  </div>
</div>

<script type="text/javascript">
  $(function() {
    var map = L.map('map',
        {scrollWheelZoom: false,
          center: [43, 0],
          zoom: 3});

    L.tileLayer('http://{s}.basemaps.cartocdn.com/light_all/{z}/{x}/{y}.png', {
        attribution: '&copy; <a href="http://www.openstreetmap.org/copyright">OpenStreetMap</a> contributors, &copy; <a href="https://cartodb.com/attributions">CartoDB</a>',
        maxZoom: 18
        }).addTo(map);

    $.get('http://csntool-v2.carto.com/api/v2/sql?format=GeoJSON&q=SELECT * FROM species_and_flywaygroups WHERE ssid=<%= @species.ssid %>',
      function(data) {
        if(data !== undefined) {
          L.geoJson(data).addTo(map);
        }
    });
  });
</script>


