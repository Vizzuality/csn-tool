.btn {
  padding: 18px 40px;
  font-family: $font-heading;
  font-size: $font-size-xs;
  font-weight: 500;
  text-decoration: none;
  text-transform: uppercase;
  letter-spacing: 0.9px;
  text-align: center;
  color: $font-main;
  background-color: $main-color;
  cursor: pointer;

  &.-inv {
    background-color: $font-main-inv;
    color: $font-main-secondary;
  }
}
